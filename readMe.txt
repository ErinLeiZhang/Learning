this is automaiton test code

how to install the request packages:

pip install -r tools\requirements.txt

#########################
how to execute the test case:

usage:
python tools/runner.py -e ci/gld  test_case_folder
eg.
python tools/runner.py -e ci test_case/onstore